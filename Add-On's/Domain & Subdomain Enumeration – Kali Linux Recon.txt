⸻

✅ Acknowledgement: Domain & Subdomain Enumeration – Kali Linux Recon
🔗 Official Reference: Hackertarget HostSearch

⸻

1) What is Subdomain Enumeration?

Subdomain enumeration is the process of identifying subdomains associated with a domain. These subdomains often host additional services (webmail, cPanel, API servers, admin panels) that expand the attack surface.

⸻

2) Why use it?
	•	Subdomains = extra entry points (often less secure than the main site).
	•	Helps understand server structure & hosting providers.
	•	Critical for OSINT & Penetration Testing workflows.

⸻

3) How does it work?
	1.	Query subdomain search APIs (e.g., Hackertarget).
	2.	Collect associated IP addresses.
	3.	Inspect HTTP headers with curl -I.
	4.	Perform WHOIS to check ownership/hosting.
	5.	Run fingerprinting (e.g., WhatWeb) to detect frameworks & versions.

⸻

4) Exact Use Case (Lab Example)

Command (subdomain enumeration):

curl -s "https://api.hackertarget.com/hostsearch/?q=example.com"

Observed Output (Example):

cpanel.example.com,93.184.216.34
webmail.example.com,93.184.216.34
ftp.example.com,93.184.216.34

Next steps:
	•	Checked headers:

curl -I http://cpanel.example.com

	•	WHOIS for IP 93.184.216.34: Hosted by IANA / Reserved for documentation.
	•	WhatWeb Fingerprinting:
	•	nginx/1.25.x
	•	Bootstrap 4.5.0
	•	jQuery 3.6.0

⸻

5) Practical Workflow
	1.	Find subdomains:

curl -s "https://api.hackertarget.com/hostsearch/?q=example.com"

	2.	Inspect headers:

curl -I http://subdomain.example.com

	3.	WHOIS check:

whois 93.184.216.34

	4.	Technology fingerprint:

whatweb example.com


⸻

6) Limitations
	•	Hackertarget API is rate-limited (too many queries → blocked).
	•	Only shows publicly indexed subdomains (not hidden ones).
	•	WhatWeb may miss obfuscated/minified JS frameworks.
	•	Professional testers often use Amass, Sublist3r, assetfinder for deeper scans.

⸻

7) Summary
	•	What: Subdomain & domain recon using Hackertarget + WhatWeb.
	•	Why: Identify hidden subdomains, hosting provider, technologies, and contact emails.
	•	How: Queried API, checked headers, fingerprinted stack.
	•	Use case shown: Found active subdomains, detected server stack (nginx + Bootstrap + jQuery), and confirmed hosting.

⸻

👉 This complements other recon tools:
	•	DMitry → WHOIS + ports.
	•	Hackertarget + WhatWeb → Subdomains + tech stack.
	•	Amass/Sublist3r → More comprehensive modern enumeration.

✅ Real-world note:
	•	DMitry = legacy/basic → good for labs.
	•	Hackertarget & modern tools = practical OSINT sources.
	•	Today, Hackertarget is often used in combination with Amass, Sublist3r, Recon-ng for professional bug bounty and pentesting.

⸻
